{% extends 'base.html.twig' %}

{% block title %}Chambres{% endblock %}

{% block body %}
    <h1>Chambres</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chambre.id }}</td>
            </tr>
            <tr>
                <th>NumChambre</th>
                <td>{{ chambre.numChambre }}</td>
            </tr>
            <tr>
                <th>NumBatiment</th>
                <td>{{ chambre.numBatiment }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ chambre.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('chambres_index') }}">back to list</a>

    <a href="{{ path('chambres_edit', {'id': chambre.id}) }}">edit</a>

    {{ include('chambres/_delete_form.html.twig') }}
{% endblock %}
